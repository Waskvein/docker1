# Начало всегда с импорта базового образа
# Используем ключевое слово 'From'
# Импортируем образ python с DockerHub
# В качестве имени образа указываем 'python', в качестве версии 'latest'
From python:latest
# Для запуска в контейнере кода написанного на питоне, импортируем файлы 'server.py' и 'index.html'
# Для этого мы Используем ключевое слово 'ADD'
# Первый параметр 'server.py' представляет имя файла, хранящегося на компьтере
# Второй параметр '/server/' это путь по которму нужго разместить указанный файл в образе
# Помещаем файл в  папку образа сервера
ADD server.py /server/
ADD index.html /server/
# Воспользуемся командой 'WORKDIR'
# Она изменяет рабочую директорию образа
# В качестве такой директории, в которой будут выполняться все команды, устанавливаем '/server/'
WORKDIR /server/
